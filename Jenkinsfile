pipeline {
  
  agent any
  
  stages {
  
    stage ('1. Checkout') {
      steps {
        echo 'Checkout'
      }
    }
  
    stage ('2. Compile') {
      steps {
         echo 'Compile'
      }
    }
  }
}
